[project]
name = "predict-api"
version = "0.1.0"
description = "Predict orchestration service"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "httpx",
    "numpy>=2.4.3",
    "pillow",
    "pydantic",
    "python-dotenv",
    "python-multipart",
    "uvicorn[standard]",
]

